<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:name="androidx.multidex.MultiDexApplication"
    android:supportsRtl="false"
    android:theme="@style/Theme.CaiNiao"
    android:usesCleartextTraffic="true"
    tools:targetApi="31">
    <activity
      android:exported="true"
      android:name=".main.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".parceldetails.ParcelDetailsActivity" />
    <activity android:name=".parceldispatch.ParcelDispatchActivity" />
    <activity android:name=".identitycode.IdentityCodeActivity" />
  </application>

</manifest>
